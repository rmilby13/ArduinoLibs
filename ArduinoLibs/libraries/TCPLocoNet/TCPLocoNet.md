- This library implements a TCP client for a LocoNet over TCP connection.
- The it is intended to connect to a JMRI Instance running LocoNet over TCP server.
- If there is not a TCP stack linked in at compile time, This library should minimize code and memory used.

